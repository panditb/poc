hazelcast:
  network:
    cluster-members:
      - 127.0.0.1:5701
    smart-routing: false
    join:
      tcp-ip:
        enabled: false
  user-code-deployment:
    enabled: true
    class-cache-mode: ETERNAL
    provider-mode: LOCAL_CLASSES_ONLY